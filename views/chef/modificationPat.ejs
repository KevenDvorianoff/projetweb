<!DOCTYPE html>
<html>
  <head>
    <title>Patrouille</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous" />
  </head>
  <body>
    <h1>Patrouille</h1>
    <p>Welcome to Patrouille</p>
    <% if (locals.alert) { %>
        <div class="alert alert-<%= locals.alert.type %>" role="alert">
            <%= locals.alert.text %>
        </div>
      <% } %>
      <% if (locals.norequest) { %>
            <%= locals.norequest.text %>
      <% } %>
      <% if (locals.results && locals.namepatrol) { %>
        <% results.forEach(element => { %>
          <%= element.NumCarte %>
          <%= element.Nom %>
          <%= element.Prenom %>
          <a href="/chef/patrouille/modifier/<%= locals.namepatrol %>/delete/<%= element.NumCarte %>">
            <button class="btn btn-danger" type="button">
                Supprimer
            </button>
          </a>
        <% }) %>
      <% } %>
      <form method="POST" action="/chef/patrouille/modifier/<%= locals.namepatrol %>">
        <input type="text" name="NumCarte" placeholder="Numéro de la carte scout" required>
        <input type="submit"> 
      </form>
      <a href="/chef/patrouille">
        <button class="btn btn-danger" type="button">
            Retour
        </button>
      </a>
  </body>
</html>