<!DOCTYPE html>
<html>
  <head>
    <title>Demande</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous" />
  </head>
  <body>
    <a href="/admin/deconnexion">
      <button class="btn btn-danger" type="button">
          Deconnexion
      </button>
    </a>
    <h1>Demande</h1>
    <p>Welcome to Demande</p>
    <% if (locals.alert) { %>
      <div class="alert alert-<%= locals.alert.type %>" role="alert">
          <%= locals.alert.text %>
      </div>
    <% } %>
    <% if (locals.norequest) { %>
          <%= locals.norequest.text %>
    <% } %>
    <% if (locals.results) { %>
      <% results.forEach(element => { %>
        <%= element.NomTroupe %>
        <%= element.NumCarte %>
        <a href="/admin/demande/accept/<%= element.NumCarte %>">
          <button class="btn btn-success" type="button">
              Accepter
          </button>
        </a>
        <a href="/admin/demande/delete/<%= element.NumCarte %>">
          <button class="btn btn-danger" type="button">
              Supprimer
          </button>
        </a>
      <% }) %>
    <% } %>
    <a href="/admin/troupe">
      <button type="button">
          Troupe
      </button>
    </a>
  </body>
</html>